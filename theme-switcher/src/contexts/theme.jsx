// import { createContext, useContext } from "react";

// export const ThemeContext = createContext({
//   themeMode: "dark",
//   darkTheme: () => {},
//   lightTheme: () => {},
// });

// export const ThemeProvider = ThemeContext;

// now we can use the useTheme hook to access the theme context in our components (no 2 lines of code needed to import useContext and ThemeContext in every component that needs to access the theme context)
// export default function useTheme() {
//   return useContext(ThemeContext);
// }
